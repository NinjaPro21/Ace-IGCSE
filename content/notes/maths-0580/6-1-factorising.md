## Core idea

Factorising is the process of breaking down an algebraic expression into a product of its constituent factors, reversing the process of bracket expansion. This can involve extracting common factors, grouping terms, or factorising quadratic expressions into double brackets. Factorising is essential for simplifying algebraic fractions and finding the roots of polynomial equations.

## Key formulas

Highest Common Factor Extraction:ab+ac=a(b+c) Difference of Two Squares:a2−b2=(a−b)(a+b)

Monic Quadratic Trinomial:x . +(p+q)x+pq=(x+p)(x+q)

## Graphs & diagrams

<div class="enlight-physics-diagram"><svg viewBox="0 0 320 220" width="320" height="220" role="img" aria-label="Quadratic parabola sketch">
      <line x1="40" y1="170" x2="290" y2="170" stroke="#64748b"/>
      <line x1="160" y1="30" x2="160" y2="190" stroke="#94a3b8" stroke-dasharray="4 3"/>
      <path d="M70 170 Q160 30 250 170" fill="none" stroke="#2563eb" stroke-width="2.5"/>
      <circle cx="110" cy="170" r="4" fill="#dc2626"/><circle cx="210" cy="170" r="4" fill="#dc2626"/>
      <text x="110" y="185" text-anchor="middle" font-size="9" fill="#dc2626">root</text>
      <text x="165" y="50" font-size="10" fill="#2563eb">a > 0</text>
    </svg></div>

Quadratic graph $y = ax^2 + bx + c$ — U-shape if $a > 0$, ∩ if $a < 0$; roots where $y = 0$.

## Steps / method

Check for a common factor across all terms.

Extract the highest common numerical factor and the lowest power of any shared variables, placing them outside a single set of brackets.

For expressions with four terms, use factorisation by grouping: factorise the first two terms and the last two terms separately to reveal a common bracket component.

To factorise a quadratic expression x . +bx+c, look for two integers that multiply to equal the constant term c and add together to equal the coefficient b.

For the difference of two squares (a2−b2), write the expression directly as the product of the sum and difference of the square roots: (a−b)(a+b).

## Examiner tip

Always check if an expression can be factorised further. For example, factorising 2x2−8 by just extracting the common factor 2 gives 2(x . −4). This is incomplete because x . −4 is a difference of two squares, which factorises further to 2(x−2)(x+2). Full marks require complete factorisation.

## Quick check

Expanding your factorised bracket answer should always bring you back to the exact algebraic expression you started with.

## Worked example — Example 1 (June 2022 P21 Q10)

Factorise completely: 6x2y−9xy2.

Identify the highest common numerical factor of the coefficients 6 and 9, which is 3.

Identify the shared variable components with their lowest appearing powers: x1 and y1.

Combine these into a single common factor to place outside the brackets: 3xy.

Divide each term of the original expression by 3xy to determine the remaining terms inside the brackets: 3xy6x2y=2x and 3xy−9xy_2=−3y.

Combine these parts to state the final factorised expression: 3xy(2x−3y).

## Worked example — Example 2 (Nov 2023 P22 Q16)

Factorise completely: x . −7x+12.

Identify that this is a quadratic expression where we need two numbers that multiply to equal +12 and add to equal −7.

List factor pairs of 12: (1,12),(2,6),(3,4). Since the sum is negative and the product is positive, both numbers must be negative.

Test the negative pairs: −3×(−4)=12 and −3+(−4)=−7. These are the correct numbers.

Place these values into a pair of linear brackets: (x−3)(x−4).
