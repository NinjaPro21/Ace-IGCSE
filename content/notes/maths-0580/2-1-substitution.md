## Core idea

Substitution is the operational process of replacing algebraic variable letters with specific numerical values within an expression or a structural formula. Once the variables are replaced, the expression is evaluated using the standard rules of arithmetic arithmetic priority (BIDMAS). Use this technique whenever you need to compute target values from real-world equations, evaluate functions at given inputs, or verify algebraic solutions.

## Key formulas

Given f(x)=mx+c,evaluation at x=k $\Rightarrow$ f(k)=m(k)+c Quadratic Evaluation:y=ax2+bx+cx=−ky=a(−k)2+b(−k)+c

## Graphs & diagrams

<div class="enlight-physics-diagram"><svg viewBox="0 0 360 120" width="360" height="120" role="img" aria-label="Function machine">
      <rect x="120" y="35" width="120" height="50" rx="8" fill="#dbeafe" stroke="#2563eb" stroke-width="2"/>
      <text x="180" y="65" text-anchor="middle" font-size="12" fill="#1e3a8a">f</text>
      <text x="60" y="65" font-size="11" fill="#64748b">x</text>
      <text x="300" y="65" font-size="11" fill="#64748b">f(x)</text>
      <line x1="75" y1="60" x2="115" y2="60" stroke="#64748b" marker-end="url(#fn)"/>
      <line x1="245" y1="60" x2="285" y2="60" stroke="#64748b" marker-end="url(#fn)"/>
      <defs><marker id="fn" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 Z" fill="#64748b"/></marker></defs>
    </svg></div>

Function machine — input $x$ maps to exactly one output $f(x)$.

## Steps / method

Rewrite the given algebraic expression or formula completely, replacing every target variable letter with empty pairs of brackets.

Insert the specified numerical values into their corresponding brackets, paying careful attention to negative signs.

Apply the order of operations (BIDMAS) by expanding terms inside brackets and calculating powers/indices first.

Perform multiplications and divisions next, then finish the calculation with additions and subtractions to find the final numerical value.

## Examiner tip

When substituting a negative number into a squared variable, always enclose the number in brackets on your calculator. For example, evaluating x . −5x when x=−3 must be entered as (−3)2−5(−3), which equals 9+15=24. Leaving out the brackets results in −9+15=6, which is a very common error that loses marks on Paper 2 and Paper 4.

## Quick check

If a negative value is substituted for a variable raised to an even power, that specific component always evaluates to a positive result.

## Worked example — Example 1 (May 2021 P21 Q4)

Given that v=u+at, calculate the value of v when u=−12, a=3.5, and t=8.

Set up the formula by writing empty brackets for the variables: v=()+()().

Substitute the given values into the brackets: v=(−12)+(3.5)(8).

Perform the multiplication step before adding: (3.5)×(8)=28.

Combine the remaining values to find the final answer: v=−12+28=16.

## Worked example — Example 2 (Nov 2022 P41 Q2a)

Calculate the value of W=53m2−2k when m=−4 and k=7.5.

Rewrite the fraction layout using brackets for variables m and k: W=53()2−2(). Insert the values into the template: W=53(−4)2−2(7.5).

Evaluate the index power first: (−4)$^2$=16, which changes the numerator to 3(16)−2(7.5).

Perform both multiplications in the numerator: 3×16=48 and 2×7.5=15. Subtract the terms in the numerator: 48−15=33.

Divide the numerator by the denominator to get the final answer: W=533=6.6.
