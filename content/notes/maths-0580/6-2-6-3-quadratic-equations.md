## Core idea

Quadratic equations are polynomial equations where the unknown variable has a maximum index power of 2. They can be solved analytically by factorising, completing the square, or using the quadratic formula. Use these methods to find the roots of quadratic equations, determine the turning points of curves, or find the x-intercepts of quadratic graphs.

## Key formulas

$$
Standard Quadratic Form:ax2+bx+c=0 The Quadratic Formula:x=2a-b\pm b2-4ac
$$

Completing the Square Form:$(x+p)^{2}$+qwhere p=2b and q=c−p2

## Graphs & diagrams

Quadratic graph $y = ax^2 + bx + c$ — U-shape if $a > 0$, ∩ if $a < 0$; roots where $y = 0$.

<div class="enlight-physics-diagram"><svg viewBox="0 0 320 220" width="320" height="220" role="img" aria-label="Quadratic parabola sketch">
      <line x1="40" y1="170" x2="290" y2="170" stroke="#64748b"/>
      <line x1="160" y1="30" x2="160" y2="190" stroke="#94a3b8" stroke-dasharray="4 3"/>
      <path d="M70 170 Q160 30 250 170" fill="none" stroke="#2563eb" stroke-width="2.5"/>
      <circle cx="110" cy="170" r="4" fill="#dc2626"/><circle cx="210" cy="170" r="4" fill="#dc2626"/>
      <text x="110" y="185" text-anchor="middle" font-size="9" fill="#dc2626">root</text>
      <text x="165" y="50" font-size="10" fill="#2563eb">a > 0</text>
    </svg></div>

Quadratic graph $y = ax^2 + bx + c$ — U-shape if $a > 0$, ∩ if $a < 0$; roots where $y = 0$.

## Steps / method

Rearrange the quadratic equation into the standard form ax2+bx+c=0 before applying any solution method.

If the expression factorises easily, set each linear bracket factor to 0 to find the roots.

For non-factorisable quadratics, use the quadratic formula.

Substitute the coefficients a, b, and c into the formula, using brackets around negative values.

Calculate the two potential solutions separately using the ± operator, and round your answers to 2 decimal places unless specified otherwise.

## Examiner tip

Paper 4 questions often include instructions like "show all your working and give your answers correct to 2 decimal places." This wording is a clear hint that the quadratic equation will not factorise, and you should use the quadratic formula. Always state your substitutions into the formula explicitly to secure method marks before calculating.

## Quick check

If the value inside the square root of the quadratic formula (b2−4ac) is negative, the equation has no real solutions.

## Worked example — Example 1 (June 2023 P41 Q3b)

Solve the equation 3x2−5x−4=0, giving your answers to 2 decimal places.

Identify the coefficients for the quadratic formula: a=3, b=−5, c=−4.

Write out the quadratic formula and substitute the coefficients carefully using brackets: x=2(3)−(−5)±(−5)2−4(3)(−4).

Simplify the terms inside the radical and the denominator: x=65±25+48 So x=65±73.

Compute the two solutions separately: x_1=65+73≈65+8.544=2.26 x_2=65−73≈65−8.544=−0.59.

## Worked example — Example 2 (Nov 2022 P22 Q11)

Express x . +6x−5 in the form $(x+p)^{2}$+q, and use this to solve x 2+6x−5=0.

Find the value of p by halving the coefficient of x: p=26=3.

Set up the squared bracket: (x+3)2. Expanding this gives x. +6x+9.

Adjust the constant term to match the original expression: we need to go from +9 to −5, so subtract 14: q=−5−3$^2$=−14. Write the completed square form: $(x+3)^{2}$−14. To solve the equation, set this form to zero: $(x+3)^{2}$−14=0 So $(x+3)^{2}$=14.

Take the square root of both sides, remembering to include both positive and negative roots: x+3=±14. Isolate x to find the exact solutions: x=−3±14.
