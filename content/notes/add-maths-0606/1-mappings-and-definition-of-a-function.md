## Core idea

A mapping is a rule that relates elements from a domain to a range. For a mapping to be defined strictly as a function, each input element in the domain must map to exactly one unique output element in the range (either one-to-one or many-to-one relationships). If an input maps to multiple outputs (one-to-many), e.g. (circles), it is not a function.

## Key formulas

$$\text{Domain: } X, \quad \text{Codomain: } Y$$

$$f: x \mapsto y \quad \text{or} \quad f(x) = y \quad \text{where } x \in X$$

## Steps / method

Identify the given domain (the set of valid inputs) and range (the target set).

Apply the vertical line test on the graph of the mapping: draw a vertical line anywhere across the domain.

If any vertical line intersects the graph at more than one point, classify it as a one-to-many mapping and conclude it is not a function.

If it passes the vertical line test, determine if it is one-to-one or many-to-one by applying a horizontal line test.

## Examiner tip

Examiners frequently test whether a function is valid over a specific domain. Always state the domain when defining a function, as a mapping can be a function under one restricted domain but invalid over an unrestricted one (e.g., $f(x) = \sqrt{x}$ is only a function for $x \ge 0$).

Do not confuse $f^{-1}(x)$ (the inverse function) with $[f(x)]^{-1}$ (the reciprocal of the function). These are entirely different and a common source of lost marks.

## Quick check

If a horizontal line crosses a function's graph more than once, then the function is classified as a many-to-one function.

## Visual / interactive intent

Reuse explorer: Function mapping diagram demo. Sliders adjust domain boundaries while an interactive mapping diagram updates to show lines connecting $x$ elements to $y$ elements, dynamically illustrating when a mapping breaks down into a one-to-many relationship.

Picture showing the relationships of different functions, e.g. one-one, one-many, many-one
