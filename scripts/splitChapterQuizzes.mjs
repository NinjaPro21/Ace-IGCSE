/**
 * @deprecated Use scripts/reimportTopicQuizzes.mjs instead.
 * That script maps quiz doc labels to each note section (not round-robin split).
 */
console.error('Use: node scripts/reimportTopicQuizzes.mjs')
process.exit(1)
